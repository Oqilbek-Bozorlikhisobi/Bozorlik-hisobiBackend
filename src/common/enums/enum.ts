export enum RoleEnum {
  ADMIN = 'admin',
  USER = 'user',
}

export enum FeedbackTypeEnum {
  SUGGESTION = 'suggestion',
  QUESTION = 'question',
  COMPLAINT = 'complaint',
  BUG = 'bug',
}

export enum CalculationType {
  ONE = 'one',
  ALL = 'all',
}
